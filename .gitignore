# Dependencies
/node_modules

# Build files
# `dist` is the LIVE deploy target (~/apps/expert -> dist) — only `build:apps`
# may write it. Every other mode writes its own non-live directory.
/dist
/dist-domainfactory
/dist-github
/dist-production

# Code quality reports
/jscpd-report

# Tool output (tools/duplicate-candidates)
/reports/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.*.local

# IDEs and editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw? 